spring.application.name=inventory-backend

# Server Configuration
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://Ayush:Ayush%408905@cluster0.lokt8ah.mongodb.net/stockmaster?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=stockmaster

# JWT Configuration
jwt.secret=StockMaster2025SecretKeyForJWTTokenGenerationAndValidation
jwt.expiration=86400000

# CORS Configuration (for Next.js frontend)
allowed.origins=http://localhost:3000,http://localhost:3001

# Logging
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.com.StockMaster.inventory_backend=DEBUG
